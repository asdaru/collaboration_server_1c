version: '3.3'
services:
  collaboration-server-v9:
    build:
      context: ./cs
      dockerfile: ./Dockerfile_v9
    ports:
      - "${SERVER_PORT}:9094"
    depends_on:
      - "postgres"
    restart: until-stopped
  postgres:
    build:
      context: ./postgres
      dockerfile: ./Dockerfile
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      POSTGRES_DB: "cs"
    restart: until-stopped
    volumes:
      - db:/var/lib/postgresql/data
volumes:
  db: